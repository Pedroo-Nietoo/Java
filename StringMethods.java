public class StringMethods{
    public static void main(String[] args){
        //String = um reference data type ("tipo de dado de referência"???) que pode armazenar um ou mais caracteres
        //reference data types possuem acesso a métodos úteis

        String nome = "Pedro";

        //boolean resultado = nome.equalsIgnoreCase("pedro"); Verifica se o nome em parênteses é igual ao nome da String

        //int resultado = nome.length(); Verifica a quantidade de caracteres no nome da String

        //char resultado = nome.charAt(2); Retorna o index de um caractere específico do nome da String, a partir de 0

        //int resultado = nome.indexOf("P"); Retorna o index do caractere que está entre parênteses

        //boolean resultado = nome.isEmpty(); Verifica se a String está vazia

        //String resultado = nome.toUpperCase(); Muda todas as letras de uma String para maúsculas

        //String resultado = nome.toLowerCase(); Muda todas as letras de uma String para minúsculas

        //String resultado = nome.trim(); Remove os espaços em branco antes e depois dos caracteres da String

       //String resultado = nome.replace("o", "a"); Substitui um caractere por outro na Strin
       
        System.out.println(resultado);
    }
}
